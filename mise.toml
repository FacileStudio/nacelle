[settings]
lockfile = true

# 1.25.0 — the floor go.mod documents for a backend importing the loop.
# Nothing in this repository needs more.
[tools]
lefthook = "latest"
go = "1.26"

[hooks]
postinstall = "lefthook install"

[tasks.check]
description = "Run the full quality gate: gofmt, vet, test, lint"
run = "sh ./scripts/check.sh"

[tasks.test]
description = "Run the tests only"
run = "go test ./..."

[tasks.format]
description = "Format the Go sources in place"
run = "sh ./scripts/check.sh --format"
